[pytest]
markers =
    regression: regression tests requiring NGC 3628 FITS at master_thesis/work_dir/NGC3628/ (deselect with -m "not regression")
    integration: marks tests requiring real cube data on disk
addopts = -m "not regression"
